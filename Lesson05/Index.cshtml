@model List<ShirtOrder>

@{
    ViewBag.Title = "Shirt Orders";
}

@section ScriptSection {
    <script>
        @{
            // TODO Lesson05 Task Solution - implement confirmDelete function here
        }
        function ConfirmDelete() {
            // Complete the code for Task 2-1
            if (confirm("do you want to delete?") == false) {
                //event.preventDefault();
                //event.stopPropagation();
                return false; 
            }
        }

    </script>

}

<h2>Shirt Orders</h2>

<span style="color:red">@TempData["Msg"]</span>
<br />
<p>
    <a asp-controller="ShirtOrder" asp-action="Create">Create New</a>
</p>

<table class="table">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Price</th>
        <th>Qty</th>
        <th>Subtotal</th>
        @if (User.IsInRole("Admin"))
        {
            <th>Created By</th>
        }
        <th>Actions</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Id
            </td>
            <td>
                @item.Name
            </td>
            <td>
                @item.Price
            </td>
            <td>
                @item.Qty
            </td>
            <td>
                @String.Format("{0:0.00}", item.Price * item.Qty)
            </td>
            @if (User.IsInRole("Admin"))
            {
                <td>@item.CreatedBy</td>
            }
            <td>
                <a asp-action="Update" asp-route-id="@item.Id">Update</a>
                @{
                    // TODO Lesson05 Task Solution - add link to navigate to ShirtOrderController Delete action
                }
                <a asp-action="Delete" asp-route-id="@item.Id" onclick="return ConfirmDelete()">Delete</a>
            </td>
        </tr>
    }
</table>
<!--19047572 Konada Obadiah Nahshon-->